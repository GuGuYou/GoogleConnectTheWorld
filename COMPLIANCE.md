# GoBuzz 数据合规文档 (Data Compliance)

> **版本**: v1.0 | **日期**: 2026-07-08 | **状态**: 技术评估阶段

---

## 1. 概述

GoBuzz 面向海外留学生群体（主要分布在 EU/UK/北美/亚洲），必须遵守相关数据保护法规。

### 适用法规

| 法规 | 适用区域 | 关键要求 |
|------|----------|----------|
| **GDPR** | EU/EEA | 数据最小化、用户同意、数据可携带、被遗忘权 |
| **UK GDPR** | 英国 | 类似 EU GDPR |
| **CCPA/CPRA** | 加州（美国） | 消费者隐私权、选择退出 |
| **中国数据出境** | 涉及中国用户 | 安全评估/认证、个人信息出境传输 |

---

## 2. 数据流图 (Data Flow Diagram)

```
┌── 客户端 (Flutter App) ──────────────────────────────────┐
│                                                            │
│  GPS 定位 (geolocator)                                     │
│    ↓                                                       │
│  GpsFuzzer.fuzz() → ~100m 精度模糊化                       │
│    ↓                                                       │
│  发送到服务端: { fuzzed_lat, fuzzed_lng, content, ... }    │
│                                                            │
│  device_token (16-char hex, 本地 SharedPreferences)        │
│    ↓                                                       │
│  AnonymousId.userIdFromToken() → sha256 hash               │
│    ↓                                                       │
│  API 请求中携带: anonymous_user_id (64-char hex)           │
│                                                            │
└──────────────────┬────────────────────────────────────────┘
                   ↓ HTTPS
┌── Cloud Run 后端 (Python FastAPI) ────────────────────────┐
│                                                            │
│  接收: { anonymous_user_id, fuzzed_lat, fuzzed_lng, ... } │
│    ↓                                                       │
│  Firestore 存储:                                            │
│    - user_id: 匿名 hash                                    │
│    - lat/lng: 已模糊 (≤100m 精度)                          │
│    - 无真实姓名、手机号、邮箱、IP                            │
│                                                            │
│  日志层:                                                    │
│    - 坐标 → grid_id（GpsFuzzer.gridId）                    │
│    - 昵称 → sha256 截断 8 位                               │
│    - 无 user_id 明文                                       │
│                                                            │
│  UGC 内容审核:                                              │
│    - 客户端第一道过滤 (ContentFilter)                       │
│    - 服务端违规词扫描                                       │
│    - 必要时机审/人审                                         │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 3. 技术措施清单

### 3.1 GPS 位置隐私

| 措施 | 实现位置 | 状态 |
|------|----------|------|
| 客户端模糊化（格网截断 + 随机抖动） | `gps_fuzzer.dart` | ✅ 已实现 |
| 服务端存储已模糊坐标 | `repositories.dart` (Mock) | ✅ 已实现 |
| 日志只记录格网 ID | `anonymous_id.dart` | ✅ 已实现 |
| 不存储原始 GPS | 全链路 | ✅ 设计保证 |

### 3.2 匿名身份

| 措施 | 实现位置 | 状态 |
|------|----------|------|
| 用户仅以 device_token 标识 | `deviceTokenProvider` | ✅ 已实现 |
| 服务端通信使用 sha256 hash | `anonymous_id.dart` | ✅ 已实现 |
| 无手机号/邮箱/真实姓名采集 | 产品设计 | ✅ 确认 |
| 不记录 IP 地址 | 服务端 | 🔴 待确认日志配置 |
| 不记录设备指纹 | 客户端 | ✅ 设计保证 |

### 3.3 UGC 内容审核

| 措施 | 实现位置 | 状态 |
|------|----------|------|
| 客户端敏感词过滤 | `content_filter.dart` | ✅ 已实现 |
| 留言可见性标记 (isVisible) | `wall_message.dart` | ✅ 已实现 |
| 服务端违规词扫描 | 后端 | 🔴 待实现 |
| 人审队列 | 后端 | 🔴 待实现 |

### 3.4 推送通知匿名

| 措施 | 状态 |
|------|------|
| 推送不包含用户身份信息 | 🔴 待确认推送模板 |
| 仅提示通用信息（"有 N 位同好"） | 🔴 待实现 |
| 分项开关（用户可选关闭） | 🔴 待实现 |

---

## 4. 待办事项 (Action Items)

### P0 - 阻塞上线

- [ ] **GDPR 法律评估**: 确认面向 EU 留学生的数据处理是否需要 DPA（数据处理协议）
- [ ] **数据出境评估**: 中国团队处理海外用户数据，是否触发中国数据出境安全评估
- [ ] **隐私政策 (Privacy Policy)**: 编写并内置于 App
- [ ] **用户同意机制**: Onboarding 阶段显示隐私政策 + 数据使用说明，获取明确同意
- [ ] **删除账户功能**: 用户请求删除时，清除所有关联数据（GDPR 被遗忘权）

### P1 - 上线前完成

- [ ] **服务端日志规范化**: 确保所有日志经过 `sanitizeLogData` 处理
- [ ] **Firestore 访问控制**: 配置 Security Rules，确保用户只能访问自己的数据
- [ ] **数据最小化审查**: 确认每个 API 接口只传输必要字段
- [ ] **第三方审核**: 对接 Google Cloud Natural Language API 做内容审核

### P2 - 持续改进

- [ ] **数据可携带**: 实现用户数据导出功能
- [ ] **Cookie/Tracker 声明**: 确认无第三方追踪（当前不采集）
- [ ] **DPIA 报告**: 数据保护影响评估（如有 EU 监管要求）

---

## 5. 风险矩阵

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| GPS 精度不足导致用户体验差 | 中 | 低 | ~100m 精度对社交场景足够 |
| 违规词表覆盖不全 | 高 | 中 | 客户端+服务端双层过滤，支持动态更新 |
| 跨境数据合规不合格 | 高 | 中 | 匿名化设计 + 法律咨询 |
| 推送内容暴露身份 | 高 | 低 | 全模板审核 + 天然匿名设计 |
